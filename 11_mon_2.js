// Initialize variables
let sum = 0;
let count = 1;

// Use a for loop to iterate and calculate the sum
for (count = 1; count <= 10; count = count + 1) {
    sum = sum + count;
}

// Display the sum
console.log("The sum of the first 10 numbers is: " + sum);
